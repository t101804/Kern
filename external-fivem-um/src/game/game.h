#pragma once
#include <cstdint>
// for debug because i'm not yet implementing the fivem features. i even not yet adding the offset lol XD
#define fivem_app L"notepad.exe"

namespace FiveM {
	//namespace offset {
	//	uintptr_t world, replay, viewport;
	//	// not setted yet
	//	uintptr_t boneList, boneMatrix = 0;
	//	uintptr_t playerInfo, playerHealth = 0, playerPosition = 0;
	//}
	void Setup();
}
